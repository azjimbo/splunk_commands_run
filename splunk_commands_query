index=_audit   ```not everyone has access to this index out of the box - there is also a datamodel that might work too - I'm playing with that```
action=search provenance="UI:Search"   ```<- change that to see results from dashboards and other search sources```
user IN("*")   ```<--put a target user in here```
NOT user IN ("splunk-system-user")
| makemv delim="|" search
| mvexpand search
| rex field=search "(?<command>\w+)\s"
| stats count by user command
| sort 0 - count
